# 一次性迁移用（复制为 .env.migration，不要提交 git）
# Supabase Dashboard → Settings → API
# 只要项目根 URL，不要加 /rest/v1/
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# 导入后图片 URL 前缀（生产部署时改成你的域名）
# MIGRATION_MEDIA_BASE=https://你的域名/api/v1/media
MIGRATION_MEDIA_BASE=/api/v1/media
